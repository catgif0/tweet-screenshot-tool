Flask==1.1.4       # Downgraded to ensure compatibility with Werkzeug and MarkupSafe
selenium==4.1.3
requests==2.26.0
imgurpython==1.1.7
Werkzeug==1.0.1    # Downgraded to ensure compatibility with Flask
MarkupSafe==2.0.1  # Version that includes soft_unicode
